<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UniLink" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>

    <style name="Theme.UniLink" parent="Base.Theme.UniLink">
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">#FF0000</item>
        <item name="android:windowBackground">@color/rojo_claro</item>
    </style>
    <style name="Theme.UniLink.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.UniLink.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.UniLink.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>